[package]
name = "mprd-models-market"
version = "0.1.0"
edition = "2021"

[dependencies]
mprd-core = { path = "../mprd-core" }
sha2 = "0.10"
serde = { version = "1.0", features = ["derive"] }

[dev-dependencies]
rand = "0.8"
proptest = "1.4"
