[package]
name = "mprd-governed-memory"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Fail-closed governance for model-proposed memory mutations"
repository = "https://github.com/TheDarkLightX/MPRD"

[dependencies]
mprd-core = { path = "../mprd-core" }
serde = { version = "1.0", features = ["derive"] }
sha2 = "0.10"
thiserror = "1.0"
