# Multi-Codex MCP Server Requirements
# ====================================
# 
# Install: pip install -r requirements.txt
#
# Note: mcp SDK is optional - server works in standalone mode without it

# MCP SDK for server mode
mcp>=0.1.0

# Optional: for async improvements
anyio>=4.0.0
